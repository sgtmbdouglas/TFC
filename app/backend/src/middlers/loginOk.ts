// import { NextFunction, Request, Response } from 'express';
// import http from '../utils/metodosHttp';

// class validBody {
//   static validation = (req: Request, res: Response, next: NextFunction) => {
//     const { email, password } = req.body;

//     if (!email || !password) {
//       return res.status(http.badRequest).json({ message: 'All fields must be filled' });
//     }
//     next();
//   };
// }

// export default validBody;
